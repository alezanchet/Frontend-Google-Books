import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 55,
    width: '90%',
    backgroundColor: '#fff',
    borderRadius: 8,
    maxWidth: 550,
    position: 'absolute',
    bottom: -25,
  },
});
